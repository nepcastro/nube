// Gen Consultores brand asset — fixed across every session, since this tool
// is Gen's own toolkit for its workshops ("de Gen para el cliente").
// Do NOT make this configurable per-session; only the client logo is uploaded.
import genLogoUrl from '../assets/gen-logo.png';

export const GEN_LOGO_DATA_URL = genLogoUrl;

export const GEN_BRAND_NAME = 'Gen Consultores';
